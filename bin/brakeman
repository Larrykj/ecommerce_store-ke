#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Removed --ensure-latest flag for CI compatibility
# Use --ensure-latest locally if you want to enforce latest version

load Gem.bin_path("brakeman", "brakeman")
