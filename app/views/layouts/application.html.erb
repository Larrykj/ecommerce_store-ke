<!DOCTYPE html>
<html>
  <head>
    <title>E-Commerce Store | Premium Shopping Experience</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Discover amazing products with our premium e-commerce platform. Fast, secure, and easy shopping experience.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>
    
    <style>
      /* Global Styles */
      :root {
        --primary-color: #667eea;
        --primary-dark: #5a67d8;
        --secondary-color: #764ba2;
        --accent-color: #f093fb;
        --success-color: #11998e;
        --warning-color: #ffc107;
        --danger-color: #dc3545;
        --dark-color: #1a1a2e;
        --light-bg: #f8f9ff;
        --glass-bg: rgba(255, 255, 255, 0.95);
        --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --navbar-height: 70px;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        background-color: var(--light-bg);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        color: #333;
        line-height: 1.6;
      }

      /* Premium Navbar Styles */
      .premium-navbar {
        background: linear-gradient(135deg, #1a1a2e 0%, #2d2d44 100%);
        padding: 0;
        height: var(--navbar-height);
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2);
        position: sticky;
        top: 0;
        z-index: 1000;
      }

      .premium-navbar .container {
        height: 100%;
        display: flex;
        align-items: center;
        gap: 30px;
      }

      .navbar-brand {
        font-size: 1.4rem;
        font-weight: 700;
        color: white !important;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .brand-icon {
        width: 40px;
        height: 40px;
        background: var(--primary-gradient);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.3rem;
      }

      .brand-text {
        background: linear-gradient(to right, #fff, #c4b5fd);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Navbar Search */
      .navbar-search {
        flex: 1;
        max-width: 500px;
      }

      .navbar-search-form {
        position: relative;
      }

      .navbar-search-input {
        width: 100%;
        padding: 10px 45px 10px 45px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.15);
        border-radius: 25px;
        color: white;
        font-size: 0.95rem;
        transition: all 0.3s ease;
      }

      .navbar-search-input::placeholder {
        color: rgba(255, 255, 255, 0.6);
      }

      .navbar-search-input:focus {
        outline: none;
        background: rgba(255, 255, 255, 0.15);
        border-color: var(--primary-color);
        box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);
      }

      .navbar-search-icon {
        position: absolute;
        left: 16px;
        top: 50%;
        transform: translateY(-50%);
        color: rgba(255, 255, 255, 0.6);
        font-size: 1rem;
      }

      .navbar-search-btn {
        position: absolute;
        right: 6px;
        top: 50%;
        transform: translateY(-50%);
        padding: 6px 16px;
        background: var(--primary-gradient);
        border: none;
        border-radius: 20px;
        color: white;
        font-size: 0.85rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .navbar-search-btn:hover {
        transform: translateY(-50%) scale(1.05);
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
      }

      /* Navbar Links */
      .navbar-links {
        display: flex;
        align-items: center;
        gap: 8px;
        list-style: none;
        margin: 0;
        padding: 0;
      }

      .nav-link-item {
        position: relative;
      }

      .nav-link-btn {
        display: flex;
        align-items: center;
        gap: 6px;
        padding: 10px 16px;
        color: rgba(255, 255, 255, 0.85);
        text-decoration: none;
        font-weight: 500;
        font-size: 0.95rem;
        border-radius: 10px;
        transition: all 0.3s ease;
        border: none;
        background: transparent;
        cursor: pointer;
      }

      .nav-link-btn:hover {
        color: white;
        background: rgba(255, 255, 255, 0.1);
      }

      .nav-link-btn.active {
        background: rgba(102, 126, 234, 0.2);
        color: #c4b5fd;
      }

      /* Cart Badge */
      .cart-badge {
        position: absolute;
        top: -2px;
        right: -2px;
        min-width: 20px;
        height: 20px;
        padding: 0 6px;
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        border-radius: 10px;
        font-size: 0.7rem;
        font-weight: 700;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
      }

      /* Auth Buttons */
      .auth-btn {
        padding: 10px 20px;
        border-radius: 25px;
        font-weight: 600;
        font-size: 0.9rem;
        text-decoration: none;
        transition: all 0.3s ease;
      }

      .auth-btn-outline {
        border: 2px solid rgba(255, 255, 255, 0.3);
        color: white;
        background: transparent;
      }

      .auth-btn-outline:hover {
        border-color: white;
        background: rgba(255, 255, 255, 0.1);
        color: white;
      }

      .auth-btn-primary {
        background: var(--primary-gradient);
        color: white;
        border: none;
      }

      .auth-btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        color: white;
      }

      /* User Dropdown */
      .user-dropdown {
        position: relative;
      }

      .user-avatar {
        width: 40px;
        height: 40px;
        background: var(--primary-gradient);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .user-avatar:hover {
        transform: scale(1.05);
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
      }

      .dropdown-menu {
        border: none;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        border-radius: 12px;
        padding: 8px;
        min-width: 200px;
        margin-top: 10px !important;
      }

      .dropdown-item {
        padding: 12px 16px;
        border-radius: 8px;
        font-weight: 500;
        transition: all 0.2s ease;
      }

      .dropdown-item:hover {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
        color: var(--primary-color);
      }

      .dropdown-item i {
        margin-right: 10px;
        width: 18px;
      }

      .dropdown-divider {
        margin: 8px 0;
        border-color: #eee;
      }

      .dropdown-item-danger {
        color: var(--danger-color);
      }

      .dropdown-item-danger:hover {
        background: rgba(220, 53, 69, 0.1);
        color: var(--danger-color);
      }

      /* Mobile Toggle */
      .navbar-toggler {
        display: none;
        padding: 8px 12px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 8px;
        color: white;
        cursor: pointer;
      }

      .navbar-toggler i {
        font-size: 1.3rem;
      }

      /* Main Content */
      .main-content {
        flex: 1;
        padding-top: 0;
      }

      /* Footer */
      .premium-footer {
        background: linear-gradient(135deg, #1a1a2e 0%, #2d2d44 100%);
        color: rgba(255, 255, 255, 0.8);
        padding: 40px 0 20px 0;
        margin-top: auto;
      }

      .footer-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 20px;
      }

      .footer-brand {
        display: flex;
        align-items: center;
        gap: 10px;
        font-weight: 600;
        color: white;
      }

      .footer-links {
        display: flex;
        gap: 24px;
      }

      .footer-link {
        color: rgba(255, 255, 255, 0.7);
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s ease;
      }

      .footer-link:hover {
        color: white;
      }

      .footer-bottom {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        text-align: center;
        font-size: 0.85rem;
        color: rgba(255, 255, 255, 0.5);
      }

      /* Alert Styles */
      .alert-custom {
        border: none;
        border-radius: 12px;
        padding: 16px 20px;
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .alert-success-custom {
        background: linear-gradient(135deg, rgba(17, 153, 142, 0.1) 0%, rgba(56, 239, 125, 0.1) 100%);
        color: #0a6c62;
        border-left: 4px solid var(--success-color);
      }

      .alert-custom i {
        font-size: 1.2rem;
      }

      /* Responsive */
      @media (max-width: 991px) {
        .navbar-toggler {
          display: block;
        }

        .navbar-search {
          display: none;
        }

        .navbar-links {
          display: none;
          position: absolute;
          top: var(--navbar-height);
          left: 0;
          right: 0;
          background: #1a1a2e;
          flex-direction: column;
          padding: 20px;
          gap: 10px;
          box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .navbar-links.show {
          display: flex;
        }

        .auth-buttons {
          flex-direction: column;
          width: 100%;
        }

        .auth-btn {
          width: 100%;
          text-align: center;
        }
      }

      @media (max-width: 767px) {
        .footer-content {
          flex-direction: column;
          text-align: center;
        }

        .footer-links {
          flex-wrap: wrap;
          justify-content: center;
        }
      }
    </style>
  </head>

  <body>
    <!-- Premium Navigation Bar -->
    <nav class="premium-navbar">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand" do %>
          <span class="brand-icon">🛍️</span>
          <span class="brand-text">E-Commerce Store</span>
        <% end %>

        <!-- Search Bar -->
        <div class="navbar-search">
          <%= form_with url: products_path, method: :get, local: true, class: "navbar-search-form" do |f| %>
            <i class="bi bi-search navbar-search-icon"></i>
            <%= f.text_field :search, 
                value: params[:search],
                class: "navbar-search-input", 
                placeholder: "Search products...",
                autocomplete: "off" %>
            <button type="submit" class="navbar-search-btn">
              Search
            </button>
          <% end %>
        </div>

        <!-- Mobile Toggle -->
        <button class="navbar-toggler" type="button" onclick="toggleNav()">
          <i class="bi bi-list"></i>
        </button>

        <!-- Nav Links -->
        <ul class="navbar-links" id="navbarLinks">
          <li class="nav-link-item">
            <%= link_to products_path, class: "nav-link-btn" do %>
              <i class="bi bi-grid-3x3-gap"></i>
              Products
            <% end %>
          </li>
          <li class="nav-link-item">
            <%= link_to categories_path, class: "nav-link-btn" do %>
              <i class="bi bi-tags"></i>
              Categories
            <% end %>
          </li>
          
          <li class="nav-link-item" style="position: relative;">
            <%= link_to cart_path, class: "nav-link-btn" do %>
              <i class="bi bi-cart3"></i>
              Cart
              <% if @cart && @cart.total_items > 0 %>
                <span class="cart-badge"><%= @cart.total_items %></span>
              <% end %>
            <% end %>
          </li>

          <% if user_signed_in? %>
            <li class="nav-link-item">
              <%= link_to new_product_path, class: "nav-link-btn" do %>
                <i class="bi bi-plus-circle"></i>
                Add
              <% end %>
            </li>
            
            <li class="nav-link-item user-dropdown dropdown">
              <a class="user-avatar dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= (current_user.name || current_user.email)[0].upcase %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li>
                  <div class="dropdown-item-text px-3 py-2">
                    <strong><%= current_user.name || 'User' %></strong>
                    <br>
                    <small class="text-muted"><%= current_user.email %></small>
                  </div>
                </li>
                <li><hr class="dropdown-divider"></li>
                <li>
                  <%= link_to orders_path, class: "dropdown-item" do %>
                    <i class="bi bi-bag-check"></i>My Orders
                  <% end %>
                </li>
                <li>
                  <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                    <i class="bi bi-person-gear"></i>Edit Profile
                  <% end %>
                </li>
                <li><hr class="dropdown-divider"></li>
                <li>
                  <%= button_to destroy_user_session_path, method: :delete, class: "dropdown-item dropdown-item-danger" do %>
                    <i class="bi bi-box-arrow-right"></i>Logout
                  <% end %>
                </li>
              </ul>
            </li>
          <% else %>
            <li class="nav-link-item auth-buttons" style="display: flex; gap: 10px; margin-left: 10px;">
              <%= link_to "Login", new_user_session_path, class: "auth-btn auth-btn-outline" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "auth-btn auth-btn-primary" %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
      <div class="container py-4">
        <% if notice %>
          <div class="alert alert-custom alert-success-custom alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill"></i>
            <span><%= notice %></span>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        
        <% if alert %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert" style="border-radius: 12px;">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>
            <%= alert %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        
        <%= yield %>
      </div>
    </main>

    <!-- Premium Footer -->
    <footer class="premium-footer">
      <div class="container">
        <div class="footer-content">
          <div class="footer-brand">
            <span>🛍️</span>
            <span>E-Commerce Store</span>
          </div>
          <div class="footer-links">
            <%= link_to "Products", products_path, class: "footer-link" %>
            <%= link_to "Categories", categories_path, class: "footer-link" %>
            <%= link_to "My Cart", cart_path, class: "footer-link" %>
            <% if user_signed_in? %>
              <%= link_to "My Orders", orders_path, class: "footer-link" %>
            <% end %>
          </div>
        </div>
        <div class="footer-bottom">
          <p>&copy; <%= Time.current.year %> E-Commerce Store. All rights reserved. Built with ❤️ in Kenya.</p>
        </div>
      </div>
    </footer>

    <script>
      function toggleNav() {
        const navLinks = document.getElementById('navbarLinks');
        navLinks.classList.toggle('show');
      }

      // Close mobile nav when clicking outside
      document.addEventListener('click', function(e) {
        const navLinks = document.getElementById('navbarLinks');
        const toggler = document.querySelector('.navbar-toggler');
        if (!navLinks.contains(e.target) && !toggler.contains(e.target)) {
          navLinks.classList.remove('show');
        }
      });
    </script>
  </body>
</html>